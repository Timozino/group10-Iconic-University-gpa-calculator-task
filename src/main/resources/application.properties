spring.application.name=group10projecticonicuniversity


# PostgreSQL Configuration
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect




#spring.datasource.url=jdbc:postgresql://localhost:5432/studentgradegpa
#spring.datasource.username=postgres
#spring.datasource.password=postgres
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Thymeleaf Settings
spring.thymeleaf.cache=false

# Server Port (optional)
server.port=${PORT:8080}

#server.port=8080

# Restrict email domain (custom logic will enforce this in the controller/service)
